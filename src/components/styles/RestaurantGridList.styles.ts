import { makeStyles } from '@material-ui/core/styles';

const useRestaurantGridListStyles = makeStyles(() => ({
  imagesContainer: {
    justifyContent: 'space-around',
    marginTop: 10
  }
}));

export default useRestaurantGridListStyles;
